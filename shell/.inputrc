# There's no reliable hook for changing the cursor between NORMAL/INSERT mode
# so stick to emacs for now. In iTerm2, we can use
# `printf '\033]1337;CursorShape=1\x7'` to change cursor types

set editing-mode emacs
set bell-style visible

# Color completions (uses $LS_COLORS)
set colored-stats on

# If completion is longer than this, replace prefix with ellipsis
set completion-prefix-display-length 12

# Complete without regard to case
set completion-ignore-case on

# Always show completions
set show-all-if-ambiguous on

# Append forward slash to both dirs and symlinked dirs
set mark-directories on
set mark-symlinked-directories on

# Do not autocomplete hidden files unless the pattern explicitly begins with a dot
set match-hidden-files off

# Show all autocomplete results at once
set page-completions off

# If there are more than 200 possible completions for a word, ask to show them all
set completion-query-items 200

# Show extra file information when completing, like `ls -F` does
set visible-stats on

# Be more intelligent when autocompleting by also looking at the text after
# the cursor. For example, when the current line is "cd ~/src/mozil", and
# the cursor is on the "z", pressing Tab will not autocomplete it to "cd
# ~/src/mozillail", but to "cd ~/src/mozilla". (This is supported by the
# Readline used by Bash 4.)
set skip-completed-text on

# Allow UTF-8 input and output, instead of showing stuff like $'\0123\0456'
set input-meta on
set output-meta on
set convert-meta off

# Use the text that has already been typed as the prefix for searching through
# commands (i.e. more intelligent Up/Down behavior)
"\e[B": history-search-forward
"\e[A": history-search-backward

# Use Alt/Meta + Delete to delete the preceding word
"\e[3;3~": kill-word

$if Python
    "\M-i": "    "
    "\M-u": "\d\d\d\d"
$endif
